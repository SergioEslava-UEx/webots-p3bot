# This property is used by the clients to connect to IceStorm.
Proxies.TopicManager = "IceStorm/TopicManager:default -p 9999"

# Proxies for publishes interfaces
Proxies.FullPoseEstimationPubPrefix = ""

# Endpoints for subscriptions interfaces
Endpoints.JoystickAdapterTopic = "tcp -p 0"
Endpoints.JoystickAdapterPrefix = ""

# Endpoints for implements interfaces
Endpoints.CameraRGBDSimple = "tcp -p 10009"
Endpoints.Lidar3D = "tcp -p 10008"
Endpoints.Camera360RGB = "tcp -p 10007"
Endpoints.KinovaArm1 = "tcp -p 10006"
Endpoints.KinovaArm = "tcp -p 10005"
Endpoints.OmniRobot = "tcp -p 10004"


Period.Compute = 5
Period.Emergency = 500

Ice.Warn.Connections = "0"
Ice.Trace.Network = "0"
Ice.Trace.Protocol = "0"
Ice.MessageSizeMax = "20004800"
